autoconf:
  - slibtool

bzip2:

codespell:
  - python

dash:

ducktape:

elfutils:
  - curl
  - bzip2

fish:
  - gettext
  - ncurses
  - pcre2

gdb:
  - ncurses
  - readline
  - zlib

gdbm:
  - readline

gettext:
  - libunistring
  - ncurses

git:
  - openssl

gnutls:
  - libidn2
  - libtasn1
  - nettle
  - p11-kit

grep:
  - pcre2

less:
  - ncurses
  - pcre2

libidn2:
  - libunistring

inetutils:

ldns:
  - openssl

libedit:

libmicrohttpd:
  - gnutls

libtasn1:

libunistring:

libyaml:

linux-pam:

neofetch:

nettle:

ncurses:

nghttp2:

openssh:
  - ldns
  - libedit
  - linux-pam
  - zlib

openssl:

p11-kit:
  - libtasn1

pcre2:
  - bzip2

procps-ng:
  - ncurses
  - systemd

python:
  - ncurses
  - openssl
  - xz

python-jinja:
  - python

python-babel:
  - python
  - python-pytz

python-markupsafe:
  - python

python-pytz:
  - python

readline:
  - ncurses

ruby:
  - gdbm
  - yaml

slibtool:

sqlite:

systemd:
  - gettext
  - openssl
  - linux-pam
  - pcre2
  - python-jinja
  - xz
  - zlib

tree:

unzip:
  - bzip2

vim:
  - ncurses

wget:
  - brotli
  - nghttp2
  - openssl
  - xz
  - zlib

xz:

zlib:

zsh:
  - gdbm
  - ncurses
